{
  "name": "tuff-plot",
  "version": "0.3.0",
  "type": "module",
  "files": [
    "*"
  ],
  "scripts": {
    "dev": "vite",
    "build": "tsc && vite build",
    "check": "tsc",
    "test": "vitest",
    "preview": "vite preview",
    "prepub": "tsc && mkdir -p dist && rm -rf dist/* && cp -a ./src/*.ts ./dist/ && cp -a ./src/*.css ./dist/ && cp ./package.json ./dist/ && cp ./README.md ./dist/",
    "pub": "npm publish ./dist"
  },
  "devDependencies": {
    "postcss-nesting": "^10.2.0",
    "typescript": "^4.6.4",
    "vite": "^3.2.3",
    "vitest": "^0.27.1"
  },
  "dependencies": {
    "@types/numeral": "^2.0.2",
    "dayjs": "^1.11.7",
    "numeral": "^2.0.6",
    "tuff-core": "latest"
  },
  "license": "MIT"
}
